{
  "name": "dev-tools",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Small tooling scripts reused across projects.",
  "bin": {
    "sync-version": "./bin/sync-version.js",
    "switch-config": "./bin/switch-config.js",
    "switch-package-source": "./bin/switch-package-source.js",
    "prepare-env": "./bin/prepare-env.js"
  },
  "files": [
    "bin"
  ],
  "volta": {
    "node": "26.8.1",
    "pnpm": "11.8.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.11",
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "@types/node": "^26.0.0",
    "c8": "^12.0.0",
    "husky": "^9.1.7",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "prepare": "husky",
    "build": "tsc -p tsconfig.build.json && chmod +x bin/*.js",
    "test": "pnpm build && node --test test/",
    "test:unit": "node --test test/unit/",
    "test:e2e": "pnpm build && node --test test/e2e/",
    "coverage": "pnpm build && c8 --all --src src --include \"src/**/*.ts\" --exclude \"src/**/*.test.ts\" --check-coverage --lines 100 --functions 100 --branches 100 --statements 100 node --test test/",
    "typecheck": "tsc",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "commitlint": "commitlint --edit"
  }
}
